version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: nmm-backend
    container_name: nmm-backend
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    restart: unless-stopped
